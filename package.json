{
  "name": "vue-mention-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "release": "yarn run lint && lerna publish --dist-tag next"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
